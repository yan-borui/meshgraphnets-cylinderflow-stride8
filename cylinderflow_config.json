{
  "protocol": "cylinderflow.stride8.prefix65.v1",
  "method": "mgn",
  "noise_std": 0.02,
  "log_every_updates": 1,
  "checkpoint_every_updates": 1000,
  "training": {
    "main": {"epochs": 25, "learning_rate": 0.0001, "microbatch": 1, "accumulation": 1, "validate_every_epochs": 1}
  }
}
